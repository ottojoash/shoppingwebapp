/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['storage.googleapis.com', 'via.placeholder.com', 'localhost'],
    },
  };
  
  module.exports = nextConfig;
  